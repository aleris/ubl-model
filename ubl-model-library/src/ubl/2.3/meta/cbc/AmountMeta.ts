import { Type } from '../Type'

export const AmountType: Type<void> = {
  name: 'Amount',
  label: 'Amount',
  definition: 'A number of monetary units specified in a currency where the unit of the currency is explicit or implied.',
  fields: new Map()
}
