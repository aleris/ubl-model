import { Type } from '../Type'

export const TextType: Type<void> = {
  name: 'Text',
  label: 'Text',
  definition: 'A character string (i.e. a finite set of characters), generally in the form of words of a language.',
  fields: new Map()
}
